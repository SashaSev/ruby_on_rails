<% if object.errors.any? %>
  <section class="errors">
    <h2>
      Oops, you can't saved
    </h2>
    <h3>
      Please check following
      <%= pluralize(object.errors.size, "error") %>
    </h3>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li>
          <%= message %>
        </li>
      <% end %>
    </ul>
  </section>
<% end %>